object teste {
  main Class Pedidos(intm valor, status)
}

val pedidos = List (
  Pedido(1, 50, "pendente"),
  Pedido(2, 200, "concluido"),
  Pedido(3, 150, "pendente")
)
  def filtrarPedidos(pedidos: List[Pedido], criterio[]): Unit = {
    pedidos.filter(criterio)
  }

  val a: Int = 10
  val pendentes: Pedido => Boolean = (p: Pedido) => p.status = "pendente"
  val acimaDeCem: Pedido => Boolean = (p: Pedido) => p.
    println(filtrarPedidos(pedidos, pendentes))
)